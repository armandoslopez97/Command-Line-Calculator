#include <iostream>
#include <string>

bool charisdig(char a); //checks if a character is a digit

bool charisop(char a); //checks if a character is an operand

bool chariscom(char a); //checks if a character is a command
